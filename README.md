# BMP-Encryptor-With-GUI

A lightweight BMP image encryption tool written in C++.

The project currently provides a fully functional command-line interface, with a GUI implementation in progress using wxWidgets.

Features:
-Encrypt and decrypt .bmp images
-CMake build support
-GUI planned (wxWidgets)
